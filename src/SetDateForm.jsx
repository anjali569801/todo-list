import React from 'react';
import SetDate from './SetDate';

function SetDateFrom (props){
  return(
    
    <SetDate onCreate={props.onCreate} onClose={props.onClose}></SetDate>
   
      
  );
}
export default SetDateForm;
